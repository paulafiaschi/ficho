import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Raúl Fiaschi - business coach",
  description:
    "Las sesiones de coaching 1:1 te brindan un espacio seguro para explorar tus metas y superar obstáculos. A través de un enfoque personalizado, lograrás una claridad que te permitirá avanzar hacia el éxito.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
